name: CI

on:
  pull_request:
  push:
    branches: [master]

jobs:
  portable:
    name: Portable core (${{ matrix.rust }})
    runs-on: ubicloud-standard-2
    strategy:
      matrix:
        rust: [stable, '1.85.0']
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ matrix.rust }}
      - run: cargo check --lib --no-default-features

  hosted:
    name: Hosted tests and guide examples
    runs-on: ubicloud-standard-2
    # a_closed_peer_reads_zero waited forever on a wake that never came and
    # held this job until it was killed. That test now bounds itself; this
    # bounds everything else. The suite runs in about nine seconds, so two
    # minutes is headroom rather than patience.
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - run: cargo fmt --all --check
      - run: cargo test --all-features
      - run: cargo clippy --all-targets --all-features -- -D warnings
