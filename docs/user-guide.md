# Nagoya user guide

The maintained guide is the [Typst source](user-guide.typ).
Run `sh scripts/build-guide.sh` to produce `docs/user-guide.pdf`.
Rust examples are extracted directly from Typst and tested by `cargo test --doc`.
